# Direct dependencies only — transitive deps (httpx, websockets, gotrue,
# postgrest, etc.) are resolved automatically by discord.py and supabase.
# Manually pinning those was causing build-resolution conflicts on deploy.

discord.py[voice]>=2.4.0,<3
PyNaCl>=1.5.0
yt-dlp                     # left unpinned: needs to stay current or YouTube breaks
python-dotenv>=1.0.0
lyricsgenius>=3.0.0
supabase>=2.0.0,<3
